class Parser:

    def __init__(self):
        pass

    def ParseInput(self, f):
        p = [list(map(int, line.split(" "))) for line in open("./a_example.in").read().split("\n") if ]
        print(p[0])

p = Parser()
p.ParseInput("./a_example.in")